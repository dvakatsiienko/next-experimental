/* Core */
import { NextPage } from 'next';

/* Components */
import { Layout, Nav } from '@/components';

const About: NextPage = () => {
    return (
        <Layout>
            <Nav title = 'About' />
        </Layout>
    );
};

export default About;
